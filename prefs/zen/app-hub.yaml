# Astra App Hub — disabled for this launch (pref gate, not a removal).
# Flip to true to restore the sidebar button, command, shortcut, panel, and
# welcome step without deleting App Hub modules or user pin data.
- name: astra.apphub.enabled
  value: false
